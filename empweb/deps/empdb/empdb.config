%% ΑΙΥΒΓΚΞΧΔΕΖΗΘΙΚΛΜΝΞΟΠΡΣΤΥΦΧΨΩαιυβγκξχδεζηθικλμνξοπρστυφχψω
[

    %%
    %% Настройка нашего веб приложения
    %%
    {empdb, [
        {cookiename,    "3m5123w38732v32"},
        {pools, [
            {emp,
                [   %% Размер пула.
                    {size, 10},
                    %% Количество соединений, которые добавляются, если
                    %% размер пула исчерапан. После того, как соединения
                    %% не нужны, они убиваются.
                    {max_overflow, 100}
                ],
                [   {hostname, "127.0.0.1"},
                    {database, "emp"},
                    {username, "root"},
                    {password, "root"}
                ]
            },
            {ejabberd,
                [   %% Размер пула.
                    {size, 10},
                    %% Количество соединений, которые добавляются, если
                    %% размер пула исчерапан. После того, как соединения
                    %% не нужны, они убиваются.
                    {max_overflow, 100}
                ],
                [   {hostname, "127.0.0.1"},
                    {database, "ejabberd"},
                    {username, "root"},
                    {password, "root"}
                ]
            }
        ]}
    ]},

    {kernel, [
        {start_pg2, true}
        %,
        %{inet_dist_listen_min, 4370},
        %{inet_dist_listen_max, 4370}
    ]}
].
